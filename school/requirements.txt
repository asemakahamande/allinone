# Production requirements for deployment

# Core Django
Django==5.2.5
python-dotenv==1.2.1

# HTTP requests (for Paystack payments)
requests==2.32.4

# Image processing
Pillow==11.3.0

# PDF generation (for report cards)
xhtml2pdf==0.2.11
python-bidi==0.4.2
reportlab==3.6.13
pypdf==6.0.0

# Charts and graphs
matplotlib==3.10.3

# Database (PostgreSQL for production)
psycopg2-binary==2.9.11

# Production server
gunicorn==23.0.0

# Static files in production
whitenoise==6.7.0
whitenoise[brotli]

# Cloud storage for media files
cloudinary==1.44.1
django-cloudinary-storage==0.3.0

# WeasyPrint (HTML to PDF) and its dependencies
weasyprint==62.3

# Email
django-anymail[brevo]